"""Extraction-accuracy evaluation harness.

Renders ground-truth label cases to synthetic artwork, runs them through the
production extraction path, and scores each extracted field against the expected
value using the same comparison logic the tool uses for its verdicts.
"""
